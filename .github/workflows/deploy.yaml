name: React app deployement

on:
  push:
    branches: main

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v3
      with:
        node-version: 16

    - name: Yarn install
      run: yarn
    - name: Deploy
      run: |
        git config --global user.email vinigonzalez1993@gmail.com
        git config --global user.name Vinícius Gonzalez Caetano
        git remote set-url origin https://vinigonzalez1993@gmail.com:${{ secrets.TOKEN }}@github.com:BISI-Capstone-Project/frontend.git
        yarn deploy
